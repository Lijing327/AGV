# 本地开发：npm run dev
# 页面示例：http://localhost:3000/agv/
# API 走相对路径 /api，由 Vite 转发到下方地址
VITE_PUBLIC_BASE=/agv/

# 不设置则 api.js 使用 /api（开发模式）
VITE_API_BASE=

# 本机 AGV 后端（与线上一致可改为 6715；若你本机仍用 8000，改回 http://127.0.0.1:8000）
VITE_DEV_PROXY_TARGET=http://127.0.0.1:6715

VITE_DEV_PORT=3000
